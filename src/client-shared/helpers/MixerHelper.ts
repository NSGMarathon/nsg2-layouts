import { Configschema } from 'types/schemas';

export const CHANNEL_LEVEL_EXPONENT = 1.5;
export const useExperimentalAccurateChannelMeters = (nodecg.bundleConfig as Configschema).x32?.useExperimentalAccurateChannelMeters ?? false;
